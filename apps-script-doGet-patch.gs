/**
 * doGet terbaru ada di:
 *   apps-script-gemini-tts.gs
 *
 * File itu berisi:
 * - getTtsConfig_()
 * - synthesizeCheckInSpeech()
 * - pcmInlineToWavBase64_()
 * - doGet(e)  ← ganti doGet lama dengan ini
 *
 * checkInByToken / processCameraData / onFormSubmit
 * JANGAN dihapus dari Code.gs.
 */
